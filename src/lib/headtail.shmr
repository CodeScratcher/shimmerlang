define(l_list, (heads, tails){
	<
		$heads = heads,
		$tails = tails,
		$car = (self){
			get_key(self, $heads)
		}
		$cdr = (self){
			get_key(self, $tails)
		}
		$
	>
})
define(l_list_end, (heads) {
	<
		$heads = heads,
		$car = (self) {
			get_key(get(self), $heads)
		}
		$cons = (self, l) {
			set_key_in(self, $tails, l)
		}
	> 
})