def($done, false)
def($loop, (){
  print("Guess? ")
  def($val, read())
  if(
    eq(int(val), 10), 
    (){
      print("Great! \n")
      set($done, true)
    },
    (){
      if(
        lt(int(val), 10),
        () {
          print("It's greater than that! \n")
        },
        () {
          print("It's less than that! \n")
        }
      )()
    }
  )()
  if(
    done,
    (){
      
    },
    (){
      loop()
    }
  )()
})
loop()