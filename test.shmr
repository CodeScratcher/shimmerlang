; def($counter, num(read("test.txt")))
; set($counter, add(counter, 1))
; write("test.txt", counter)

def(
  $factorial,
  (n) {
    if(
      eq(n, 1),
      1,
      mul(
        n,
        factorial(sub(n, 1))
      )
    )
  }
)

print("Result:", factorial(5), "\n")